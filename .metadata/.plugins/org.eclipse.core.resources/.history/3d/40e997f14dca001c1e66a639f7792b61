/*
 * Informar.c
 *
 *  Created on: 2 may. 2022
 *      Author: miguel
 */

#include "Informar.h"


int	total_promedio_CantidadMayorPromedio(eAlbum* pAlbum , int tamAlbum , int* total , int* promedioImportes , int* cantidadMayorPromedio)
{
	int estado = -1;
	int i, j;
	int contador = 0;
	int acumulador = 0;
	int auxPromedio ;
	int contadorMayorPromedios = 0;
	if(pAlbum != NULL && tamAlbum > 0)
	{
		for (i = 0; i < tamAlbum; ++i) {

			if(pAlbum[i].importe > 0)
			{
				contador++;
				acumulador += pAlbum[i].importe;
			}
		}
		*promedioImportes = acumulador /contador;
		*total = acumulador ;

		for (j = 0; j < tamAlbum; ++j) {

			if(*promedioImportes < pAlbum[j].importe)
			{
				contadorMayorPromedios++;
			}
		}


	}

	return estado;
}
